#include <stdio.h>

int main() {
    // Please write your code here.
    int width, height;
    scanf("%d %d", &width, &height);
    a += 8;
    b *= 3;
    printf("%d\n%d\n%d", width, height, width * height);
    return 0;
}